# 2026-09-13：D31 至 D35 协调批与 v1.0.0 发版

> ohmycloud WSL 总台外部协调三件开工，日内扩容两件（D34 版本线重开、D35 README 与仓库描述）；herdr codex 三轮独立评审达成一致后封版。方案与过程见 `docs\proven\P0048`；本篇记流水与自省。

## 流水

1. **立项（4848bb9）**：来函三件入 PRD（D31 域名清扫、D32 旗标连字符化、D33 yolo 分级），GOAL/PLAN/TODO 随批。
2. **D31（1dff822）**：diagnose.rs 注释加 README 加 R002 2.5 加 P0041 四处私有域名脱敏，表述统一「api 缓存回归测试端点（配置注入）」；代码端点本就 env 加 agent 配置注入无硬编码，逻辑不动；全仓 grep 复扫归零。
3. **D32（41a03fd）**：clap 显式 `long = "pre-trust"` 加隐藏 `alias = "pretrust"`；kv 标记 `init.pretrust.*` 冻结不动（ohmycloud 消费）；md-replace 全仓 28 处更正含历史档案（旧拼写仍为可用别名，命令逐字可复跑无失真）；集成测试双跑新拼写与别名锁 help 面。
4. **D33（7504de3 + 061ecf5）**：`YoloLevel` 取值式旗标两级（`--yolo[=full|partial|off]`、`--project-yolo[=级别]`，裸旗标兼容、缺省 full）；四家 partial 取值 claude `acceptEdits`、codex `workspace-write`/`on-request`、kimi `auto`、grok `auto`（grok-build `permissions.rs` canonical 值集钉死，S007 缺口收口）；off 按 ours 等值摘除（新 `retire_user_yolo_with`，项目面扩认两代值）；doctor 分级判据；COMMAND_MAP 加 R002/R007/R001/AGENTS/INDEX/README 随批；dogfood init 四处 SKILL 再生。
5. **D34（1e5efd3 加 eaaae84）**：先发 0.6.2 号后用户裁版本线重开，改 1.0.0（与 ark 1.0.0 同语义开山版）；CHANGELOG 版本线注记、v0.6.x 转更名过渡期记录、活文档 v0.7 兼容窗改 1.1.0。教训：发版号在 push 前应等齐补充裁定的窗口。
6. **评审闸门（db3c8a1、38b1e35、ed94f94）**：用户令 herdr 同步右侧 codex 达成一致才封版。首轮：D31/D32/D34 零异议，D33 F1（full 降 partial 不摘 ours 落的 MCP 审批键）加 F2/F3 轻量加根 SKILL.md 编排遗物观察；F1 裁 a 落修加降级回归测试。二轮：F1 修闭环，新 A（enabledMcpjsonServers 名单残留）加 B/C/D/E；A 裁改口径（名单含 agent 原生用户审批无法归因，一律保留），B trace 分页句、C 示例可复跑形、D canonical 仓名 `hst_rs`、E clap 帮助补边界。三轮终局：全核实、门禁全绿、两处纯装饰残留不阻塞 tag，达成一致。
7. **D35（38b1e35 起）**：README 精简重写（安装含镜像直链、部署与信任、诊断验收、活性诊断、状态栏、trace、注意），GitHub 仓库描述一句话（`gh repo edit` 已落）。
8. **发版**：v1.0.0 tag 打在评审终批 `ed94f94`；CI 两轮绿、十二资产齐（hst 六件加 oma stub 六件）、镜像 hst/stable 与 oma/stable 段随 tag 到货核验、herdr 知会 ohmycloud（版本加资产 sha256）。
9. **当日补丁 D37（verify 状态栏层环境探测）**：ohmywsl 总台（全新 Ubuntu 24.04、四家已装、`hst init --pre-trust` 落盘）跑仓测红一项：codex 状态栏层必红（`[tui] status_line` 归 `hst statusline` 面、init 不写）加无 pwsh 机器三家属红。裁定 verify 状态栏层只验已部署的面：未部署或缺可选运行时 = skip 不计败带 CTA，断链仍 fail；hook 层保持严格（登录缺失红属真阳性）。162 单测加 27 集成绿。
10. **当日补丁 D38（活体验收环境容错，三轮到点）**：总台第二轮断言原文回传，红的是 grok 的 hook 层（`verify.grok=fail(statusline=ok)`，CLI 已装无凭据，环境事实非 bug）：grok 无头会话先鉴权才触发 hook。第 1 轮修 = 登录文件形状闸门，总台第三轮断言再贴证明没修到点（凭据文件在场但不可用，形状探测放行后照红）；第 2 轮终版 = 拆闸门改结果容错：grok/kimi 跑一次不押断言、hook 层 ok 才计入活体断言、不成打 skip 带首条 reason，claude/codex 硬断言保持，产品 verify 面不动（如实 fail 修环境归操作员），R004 三.6 口径随批。自省两刀：验收面板消费仓测绿、操作员消费产品面，两层红绿口径分开裁；环境探测可靠性排序 = 结果本身 > 状态文件形状 > 文件在场，来函要 skip 时别自作聪明选更聪明的探测层。
11. **当日补丁 D39（Windows hook 注册 sh 兼容形态，三轮）**：宿主实弹来函（claude 的 hook 执行 shell 是 POSIX sh，直路径 .cmd 不认盘符，对方手动 `cmd.exe /c` 救济 rc=0）。第一反应照来函字面包 `cmd.exe /c`，本机 live verify 立刻抓红：Git Bash 的 MSYS 参数转换把 `/c` 吃成盘符路径（rc=1 无落盘）；`//c` 转义形在 PS 又进交互态。取证矩阵（M062）：MSYS 与 WSL interop 对斜杠开关的转换方向相反，三种前缀无跨 sh 通吃解，唯「按名解释器加参数位路径」形通吃，取 ps1 桥加新增 ps1 版状态 shim（无 BOM 落盘防 serde 炸），三 shell 实弹 rc=0、live verify 四家绿。第 2 轮（宿主终验回执）：ps1 桥已生效 sh 实跑 rc=0，但幂等漏（加新不清旧，宿主手清 8 条）：is_ours 解释器头用全 token 精确匹配漏认全路径头、陈旧替换只在自管事件集内跑漏非管理事件（M063）；修法 = 头判定按 stem 分派 + 非管理事件清扫。第 3 轮（用户点破漏跑评审闸门，补送 codex）：F1 注册头无 `.exe` 在 WSL 形 rc=127（interop 不做 PATHEXT，原故障未修尽）改 `powershell.exe` 头、F2 doctor 判形取首 token 把新形态全误报 shim-dead 抽 program_token 共用、F3 ps1 控制台编码吃中文改字节态读、F4 事件映射对齐 sh 版、F5 codex 清扫外科摘键保异侧活注册、F6 包裹分支收窄防外来前缀族误伤；四态实测 rc=0、本机活体 doctor 四面 form=shim 全绿。第 4 轮（codex 二轮复核）：G1 抓出 F3 读法自身是回归（OpenStandardInput 重定向下从流末尾读恒空），且我方复测又追出第三层根因 = ps1 本体必须带 UTF-8 BOM（无 BOM 被 PS 5.1 按 ANSI 解析，中文注释乱码破坏语句、殃及读法），终版 = BOM 本体加 $input 惰性枚举加读前 InputEncoding 钉 UTF-8，中文载荷三态往返全对；G2 verify 判据补 event 非空（此前空解析假绿）加直管道往返集成钉；G3 去 (?i)；G4 负例断言。自省四刀：来函给的正形态在自己机器有失败实证时先跑证据矩阵再落（桥接目标听来函的、载体听实证的）；「按名解析」论断要连扩展名一起验（WSL 无 PATHEXT）；评审闸门是流程债，漏跑被用户点破的代价是返工两轮；PS 5.1 的 stdin 与编码是三层套娃（宿主预读、ANSI 解析无 BOM 脚本、输出文件又要无 BOM），每层都要单独实证不能推理。
12. **v1.0.1 发版与宿主全链闭环**：宿主反馈 v1.0.0 旧二进制无清扫逻辑，重跑 init 又落直路径行双注册再手清止痛，依赖新版二进制终验；发 v1.0.1（tag 钉 157ed99，CHANGELOG 收 D37 至 D39），镜像双段到货、win digest 三方对账全等（b884da426587）、herdr 知会 ohmycloud。宿主终验回执：装新版重跑 init，八事件全收敛单条 ps1 正形态、裸 cmd 零残留、sh 终弹 rc=0，「hook 形态线全链闭环」。自省：源码修复对二进制消费者要记「交付面 = release」，修在 main 不滚 stable 宿主的 init 永远是旧版；今后宿主终验类协调，修复合入即问是否随批发版。
13. **D40 状态栏双排与三要素（wsl 总台三令）**：用户补充裁定双排分组后同日落完。双排 = segments/segments2 两清单加断点重置收集器（`$parts` 复用零段块改动）加 single_line 逃生门（kimi/grok 多行未实证的兜底）；三要素 = tools 段（transcript tool_use 计数）、mcp 段（stdin mcp_servers 优先回落配置文件键数）、context `{mix}`（transcript 尾段行字符量三分估算，无真实 token 计量的近似口径明示）；CTXPROBE 按消费方门控与 COMMON/PROBE 同款纪律；codex 对齐 = 源码取证 status_line_setup.rs 约 30 内置项、无外部命令面（#17827/#20244），缺省集升十二项用 token 细分承载「token 用量」要素，tools/MCP/构成三要素明示不可达。自省：官方 payload 给不了的字段就明示近似口径（transcript 估算是 HUD 级精度，不是计费级）；能力边界要源码取证而不是文档猜测（learn.chatgpt.com 的 config reference 连项清单都不列）。
14. **D42 状态栏三行重分组（用户裁定双排版嫌丑）**：样例「pwsh 加目录加 claude:working 加模型加构成加分支换行后工具数加 token 加时长」被嫌丑，用户裁三行定名（项目状态 / agent 状态 / 运行时状态，第三行用户补名）；segments3 第三行键加拼装器泛化为任意行数（ps1_rowsplit(n) 加 ps1_tail_multi），机读标记从「首行含」放宽为「任一行含」（agent 态迁二行，S025 契约与 verify 消费面随批，live verify 抓红后即时修正）；kimi 并一行断言顺带收 codex D40 评审挂账 G2 的一半。自省：布局重设计动的是消费契约（首行标记）时，先全链扫消费方（verify 加 grep 消费面加 S025 契约文档）再落，不然 live 测试当场抓红。

## 自省

- **评审闸门有效**：F1 是真语义缝（降级不彻底），我首版只做了「不写」没做「摘除」；codex 的降级路径测试建议暴露了 fresh-only 断言盲区。三轮收敛快是因为每轮把裁定的口径写回文档再复核，而不是只改代码。
- **版本号要等齐裁定窗口**：0.6.2 的 chore 提交已推后才来 D34 裁定，多花一笔改号加全仓口径批。发版号这类全局串，push 前显式问一句「还有没有补充裁定」更稳。
- **跨仓协调批的天然节拍**：来函即澄清的需求按 PRD 编号走标准链不打折；封版闸门（评审一致）与补充裁定（版本线、需求四）以用户令插入，都能收敛进同一批。
